if Global.game_settings and Global.game_settings.level_id == "pbr" then
	function AchievmentManager:award(id, ...)
		return
	end
	function AchievmentManager:award_progress(stat, value, ...)
		return
	end
end
